//AGE specific version string, piggybacked onto MAME's own string.
//However, this does not work ingame, as APPNAME is defined in the main body of the source,
//where it can't be touched. Hopefully, some arrangement to fix this can be found soon.
#define AGEVER			"0.40"
